<Window x:Class="TidyFlow.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TidyFlow.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:PreviewViewModel}"
        Title="Preview changes - TidyFlow"
        Width="900" Height="600" MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Icon="/Assets/icon.ico">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Heading}" Style="{StaticResource SubtitleTextBlockStyle}"/>
        <TextBlock Grid.Row="1" Text="{Binding SubHeading}" Margin="0,4,0,12" TextWrapping="Wrap"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding View}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" SelectionMode="Extended"
                  AutomationProperties.Name="Planned changes">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="2*"/>
                <DataGridTextColumn Header="What happens" Binding="{Binding Action}" Width="2*"/>
                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="90"/>
                <DataGridTextColumn Header="Destination" Binding="{Binding Destination}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <CheckBox Content="Show files that will stay where they are" IsChecked="{Binding ShowSkipped}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Close" IsCancel="True" MinWidth="90" Margin="0,0,8,0"/>
                <Button Content="{Binding OrganizeButtonText}" Style="{StaticResource AccentButtonStyle}"
                        IsDefault="True" IsEnabled="{Binding HasMoves}" MinWidth="120" Click="Organize_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
